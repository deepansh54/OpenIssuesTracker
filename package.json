{
  "name": "openissuestracker",
  "version": "1.0.0",
  "description": "Open Issue tracking for public git repositories",
  "repository": "https://github.com/deepansh54/openissuestracker",
  "license": "MIT",
  "dependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1",

    "preupdate-webdriver": "npm install",
    "update-webdriver": "webdriver-manager update"
  }
}